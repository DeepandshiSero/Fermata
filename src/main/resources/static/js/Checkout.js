document.querySelector('.checkout-btn')?.addEventListener('click', () => {
    // TODO: hook up to a /checkout POST endpoint once payment flow exists
    console.log('Proceed to payment clicked');
});